import { createTheme } from "@mui/material/styles";
const theme = createTheme({
    palette: {
        primary: {
            main: "#EC6724",
        },
        background: {
            paper: "#FFFFFF",
        },
    },
});
export default theme;
